fastapi
uvicorn[standard]
supabase
python-jose[cryptography]
PyJWT[cryptography]
pydantic-settings
pydantic
python-multipart
Pillow
opencv-python-headless
huggingface_hub        # downloads Aurora skin model at startup (inference mode)
# TensorFlow — required for MODEL_MODE=inference. Demo mode works without it.
# Requires Python 3.9–3.12.
# CPU-only:  pip install tensorflow-cpu==2.16.1
# GPU:       pip install tensorflow==2.16.1
pytest==8.2.2
pytest-mock==3.14.0
httpx==0.27.0
python-dotenv==1.0.1
